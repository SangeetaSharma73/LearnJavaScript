🧠 What is Currying? (One-line, Simple)

Currying means converting a function that takes multiple arguments into a chain of functions that take one argument at a time.

🧩 Normal Function (Not Curried)
function add(a, b, c) {
return a + b + c;
}

add(1, 2, 3); // 6

➡️ All arguments passed together

🔥 Curried Version (Same Function)
function add(a) {
return function (b) {
return function (c) {
return a + b + c;
};
};
}

add(1)(2)(3); // 6

➡️ Arguments passed one by one

🏠 Real-Life Example (Best Way to Remember)

Imagine ordering food 🍔

❌ Normal Way

“Give me burger + fries + coke”

✅ Currying Way

“Give me burger”
“Now add fries”
“Now add coke”

➡️ Step-by-step order
➡️ Each step remembers the previous one

🧠 Why does this work? (Lexical Scope + Closure)
function outer(a) {
return function inner(b) {
console.log(a + b);
};
}

outer(5)(3); // 8

inner remembers a

This memory is called closure

Currying uses closure

✨ Easy Example: Multiply
function multiply(a) {
return function (b) {
return a \* b;
};
}

multiply(2)(5); // 10

Reusable Functions (Power of Currying 🔥)
const double = multiply(2);
double(10); // 20

const triple = multiply(3);
triple(10); // 30

🧩 Arrow Function Version (Clean)
const add = a => b => c => a + b + c;

add(1)(2)(3); // 6

🧪 Partial Application Example
function discount(discountPercent) {
return function (price) {
return price - price \* discountPercent;
};
}

const tenPercentOff = discount(0.1);
tenPercentOff(500); // 450
tenPercentOff(1000); // 900
